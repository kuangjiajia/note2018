##### class

````javascript
//es5
function Person(name,age) {
    this.name = name
  	this.age = age
}
Person.prototype.sayName = function() {
    console.log(this.name)
}
var llp = new Person("llp",20)
llp.sayName() // "llp"

//es6
class Person {
    constructor(name,age) {
        this.name = name
      	this.age = age
    }
    sayName() {
        console.log(this.name)
    }
}
var llp = new Person("llp",20)
llp.sayName() // "llp"

//语法糖
````

##### 继承

```javascript
//es5
function Person(name,age) {
    this.name = name
  	this.age = age
}
Person.prototype.sayName = function() {
    console.log(this.name)
}

function Student(name,age) {
   Person.apply(this,name,age)
}
Student.proto
```



##### 函数

1.私有方法，私有属性，共有属性，共有方法，公有属性，公有方法，构造方法

```
var Book = function(id,name,price) {
	//私有方法,私有属性
	var num = 1
	function getName() {}
	//公有属性
	this.id = id
    this.name = name
    this.price = price
    //公有方法
    this.sayName = function() {console.log(this.name)}
    this.setName = function(n) {this.name = n}
    //构造方法
    this.setName("kjj")
}
共有属性和共有方法

Book.prototype = {
	getAge: function() {
      return this.name
	},
	getPrice: function() {
      return this.price
	},
	hobby: "sleep"
}
```

2.闭包

```javascript
var Book = function() {
    var name = 'kjj'
    var getName = function() {
        return name
    }
    return getName
}

//第一秒为1，第二秒为2....
for(var i = 0 ; i < 10 ; i++ ) {
	(function(i) {
		setTimeout(function() {
			console.log(i+1)	
		},1000 * i)	
	})(i)
}

for(let i = 0 ; i < 10 ; i++ ) {
	setTimeout(function() {
		console.log(i+1)
	},i * 1000)
}

for(var i = 0 ; i < 10 ; i++ ) {
	setTimeout(function(i) {
		console.log(i+1)
	},1000 * i, i)
}

```

3.this

函数调用

```Javascript
var fun1 = function() {
  console.log(this)
}
func1()
var fun2 = function() {
  'use strict'
  console.log(this)
}
```

方法调用

```
var obj = {
  func: function() {
    console.log(this)
  }
}
obj.func()
```

构造函数

```
var Person = function(name,sex) {
  this.name = name
  this.sex = sex
  this.getContext = function() {
    return this
  }
}
var llp = new Person("llp","boy")
llp.getContext()
```

apply,call进行调用

```
var Person = function(name,age,sex) {
  this.age = age
  this.sex = sex
  this.name = name
  this.sayName = function() {
    console.log(this.name)
  }
}

var Student = function(age,sex,name) {
  Student.call(this,name,age,sex)
}

var zdm = new Student(20,"male","zdm")
zdm.sayName()
```

bind改变上下文

```
var obj1 = {
  name: "llp"
}
var obj2 = {
  name: "kjj",
  getName: function() {
    console.log(name)
  }.bind(this)
}
```

##### 原型链

对象都是函数创建的(构造函数)

```javascript
var llp = {
  name: "llp",
  age: 20
}
var llp = new Object()
llp.name = "llp"
llp.age = 20
```







##### 数组方法

```javascript
//es5
// filter map forEach pop push shift unshift slice splice 

//es6
// entries keys values includes fill find findIndex copyWithin Array.of Array.from

const filter = (arr,fn) => {
  	const newArr = []
    for(let i = 0, len = arr.length; i < len ; i++) {
        if(fn(arr[i])) newArr.push(arr[i])
    }
	return newArr
}

const map = (arr,fn) => {
    const newArr = []
    for(let i = 0,len = arr.length ; i < len ; i++) {
        newArr.push(fn(arr[i]))
    }	
    return newArr
}

const forEach = (arr) => (fn) => {
    const newArr = []
    for(let i = 0,len = arr.length ; i < len ; i++) {
        fn(arr[i])
    }
}
```

##### Set Map

````javascript

````

##### http

